# Server
PORT=4000

# MariaDB
DB_HOST=localhost
DB_USER=root
DB_PASS=admin
DB_NAME=aquagarden
DB_PORT=3306

# JWT (backend local)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=8h

# Firebase: servicio (serviceAccountKey.json) en config/
# No se ponen las keys aquí si usas serviceAccount json.
FIREBASE_ENABLED=true

# MQTT
MQTT_ENABLED=false
MQTT_BROKER_URL=mqtt://broker.hivemq.com
MQTT_USERNAME=admin
MQTT_PASSWORD=123456
MQTT_TOPICS=sensors/#

# Otros
LOG_LEVEL=debug
